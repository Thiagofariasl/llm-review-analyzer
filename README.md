# llm-review-analyzer
Python project that uses a Large Language Model (LLM) to analyze app reviews, translate them to Portuguese, classify sentiment (positive, negative, neutral), and convert the results into structured JSON data.
